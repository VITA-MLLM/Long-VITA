from setuptools import find_packages, setup

setup(
    name='long_vita',
    version='0.0.1',
    packages=[
        "long_vita",
        "long_vita_modellink",
        "long_vita_megatron",
    ],
    install_requires=[
    ],
)

